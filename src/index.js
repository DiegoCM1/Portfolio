import React from 'react';
import ReactDOM from 'react-dom';

function App() {
  return <h1>Hello, this is React inside your existing project!</h1>;
}

ReactDOM.render(<App />, document.getElementById('root'));
